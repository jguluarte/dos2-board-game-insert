# Build the uv-managed virtualenv if it's missing, then activate it.
# (Python + pinned deps come from pyproject.toml.)  Run `direnv allow` to enable.
if [[ ! -d .venv ]] && has uv; then
    uv sync
fi
[[ -d .venv ]] && source .venv/bin/activate

export REPO_ROOT=$PWD
